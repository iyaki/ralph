// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ubuntu
{
	"name": "Ralph Devcontainer",
	"image": "mcr.microsoft.com/devcontainers/universal:5-linux",

	"runArgs": [
		"--env-file", "${localWorkspaceFolder}/.env",
		"--userns=keep-id", // For podman users, to avoid permission issues with bind mountson on rootless mode.
		"--memory", "8g"
	],

	"mounts": [
		// OpenCode config and data - https://opencode.ai/docs/cli/#configuration-and-data
		{
			"source": "${localEnv:HOME}/.opencode",
			"target": "/home/codespace/.local/share/opencode",
			"type": "bind"
		},
		{
			"source": "${localEnv:HOME}/.local/state/opencode",
			"target": "/home/codespace/.local/state/opencode",
			"type": "bind"
		},
		// Global vercel skills config
		{
			"source": "${localWorkspaceFolder}/.agents/.skill-lock.json",
			"target": "/home/codespace/.agents/.skill-lock.json",
			"type": "bind"
		},
		// GitHub CLI config
		{
			"source": "${localEnv:HOME}/.config/gh/",
			"target": "/home/codespace/.config/gh/",
			"type": "bind"
		},
		// Override Git LFS artifacts script to avoid conflicts with pre configured git hooks (e.g, lefthook).
		{
			"source": "${localWorkspaceFolder}/.devcontainer/pull-git-lfs-artifacts-override.sh",
			"target": "/usr/local/share/pull-git-lfs-artifacts.sh",
			"type": "bind"
		},
		// Cache Go modules in a volume to speed up builds and avoid redownloading dependencies.
		{
			"source": "${localWorkspaceFolderBasename}-gopath",
			"target": "/go",
			"type": "volume"
		}
	],

	"features": {
		"ghcr.io/devcontainers/features/common-utils:": {
			"installZsh": false,
			"installOhMyZsh": false,
			"installOhMyZshConfig": false,
			"username": "automatic"
		},
		"ghcr.io/devcontainers/features/go:": {
			"version": "1.25"
		},
		"ghcr.io/devcontainers-extra/features/shfmt:1": {},
		"ghcr.io/devcontainers-extra/features/npm-packages:1": {
			"packages": "repomix"
		},
		"ghcr.io/devcontainers-extra/features/opencode:1": {},
		"ghcr.io/iyaki/devcontainer-features/lefthook:": {}
	},

	"customizations": {
		"vscode": {
			"extensions": [
				"editorconfig.editorconfig",
				"golang.go",
				"nhoizey.gremlins",
				"sst-dev.opencode"
			]
		}
	},

	"initializeCommand": "${localWorkspaceFolder}/.devcontainer/setup.sh",
	"postCreateCommand": {
		"Install npm packages": "npm install",
		"Install go tools": "/bin/bash .devcontainer/install-go-tools.sh",
		"Setup lefthook": "lefthook install"
	},
	"postStartCommand": {
		"Refresh opencode available models": "opencode models --refresh"
	},

	"forwardPorts": [
		// Add your own ports here
		4096
	],
	"portsAttributes": {
		"4096": {
			"label": "Default OpenCode server"
		}
	}

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
